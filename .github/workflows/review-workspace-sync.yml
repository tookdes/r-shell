name: Review workspace sync

on:
  push:
    branches:
      - review/codex-workspace-sync
  workflow_dispatch:

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout review branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Frontend checks
        id: frontend
        run: |
          pnpm build
          pnpm test
          pnpm i18n:check

      - name: Rust checks
        id: rust
        working-directory: src-tauri
        run: |
          cargo fmt --check
          cargo test
          cargo clippy -- -D warnings

      - name: Record verification result
        if: always()
        run: |
          printf 'frontend=%s\nrust=%s\n' '${{ steps.frontend.outcome }}' '${{ steps.rust.outcome }}' > .review-ci-status
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .review-ci-status
          if ! git diff --cached --quiet; then
            git commit -m 'chore: record review CI status [review-ci]'
            git push
          fi

      - name: Fail when verification failed
        if: always() && (steps.frontend.outcome != 'success' || steps.rust.outcome != 'success')
        run: exit 1
